Metadata-Version: 2.4
Name: fault-diagnosis-agent
Version: 0.1.0
Summary: LangGraph-based RAG agent for oil and gas field fault diagnosis.
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: fastapi>=0.111.0
Requires-Dist: uvicorn[standard]>=0.30.0
Requires-Dist: pydantic>=2.7.0
Requires-Dist: python-docx>=1.1.2
Requires-Dist: langgraph>=0.2.0
Requires-Dist: langchain-core>=0.2.0
Requires-Dist: rank-bm25>=0.2.2
Requires-Dist: jieba>=0.42.1
Provides-Extra: llm
Requires-Dist: langchain-openai>=0.1.20; extra == "llm"
Provides-Extra: dev
Requires-Dist: pytest>=8.2.0; extra == "dev"
Requires-Dist: httpx>=0.27.0; extra == "dev"

# 油气田故障诊断智能问答 Agent

> 基于 FastAPI + LangGraph 的油气田设备故障诊断 RAG Agent——支持压缩机、分离器、干管、集气站外输和气井积液等场景，开箱即用，也支持接入任意 OpenAI 兼容的 LLM 增强推理。

[![Python 3.10+](https://img.shields.io/badge/python-3.10+-blue.svg)](https://www.python.org/)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.115+-009688.svg)](https://fastapi.tiangolo.com/)
[![LangGraph](https://img.shields.io/badge/LangGraph-1.x-1c3c3c.svg)](https://langchain-ai.github.io/langgraph/)

## ✨ 功能特性

- **/diagnose**：输入自然语言故障描述 → 返回根因分析、处置步骤、风险提示和参考依据。
- **/diagnose/trace**：完整 RAG 管线追踪接口，可观察实体识别、分类、检索、推理的每一步过程。
- **/kb/stats** / **/kb/items** / **/kb/items/{id}**：知识库统计与详情查询。
- **/feedback**：记录用户对诊断答案的评分和意见，便于持续优化。
- **Word 知识库导入**：将《知识.docx》解析为结构化 JSON 知识库。
- **LangGraph 状态机**：分类 → 信息补全判断 → 检索 → 根因分析 → 方案生成 → 最终回答。
- **可选 LLM**：未配置 API Key 时也能用本地规则和知识库跑通，便于离线演示；配置后自动增强根因分析。
- **自由切换 LLM 服务商**：兼容所有 OpenAI 兼容接口的服务（DeepSeek、智谱、Kimi、通义千问、Ollama 等），无需改代码。

## 🚀 快速开始

### 环境准备

```bash
python -m venv .venv
.venv\Scripts\activate          # Windows
# source .venv/bin/activate     # macOS / Linux

pip install -r requirements.txt
```

> 如需启用 LLM 增强，额外安装：`pip install langchain-openai`

### 启动服务

```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

打开接口文档：http://127.0.0.1:8000/docs

### 测试诊断

```bash
# Windows PowerShell
curl -X POST http://127.0.0.1:8000/diagnose ^
  -H "Content-Type: application/json" ^
  -d "{\"query\":\"压缩机压力过高报警怎么办？\",\"session_id\":\"test123\"}"
```

## 📚 导入真实 Word 知识库

将你的 `知识.docx` 放入 `data/raw/` 目录后执行：

```bash
python scripts/build_knowledge_base.py --doc data/raw/知识.docx --output data/processed/fault_knowledge.json
```

脚本会按标题和步骤抽取知识块，并写入 API 默认读取的知识库文件。

## 🤖 配置 LLM（可选）

本项目不强制绑定任何 LLM，默认走本地规则推理。若需要 LLM 增强根因分析，复制 `.env.example` 为 `.env` 后填入：

```bash
# 复制模板
cp .env.example .env
```

然后在 `.env` 中填写（任选一家）：

### 方案 1：OpenAI
```
LLM_API_KEY=sk-your-openai-key
LLM_BASE_URL=
LLM_MODEL=gpt-4o-mini
```

### 方案 2：DeepSeek
```
LLM_API_KEY=sk-your-deepseek-key
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat
```

### 方案 3：智谱 AI (GLM)
```
LLM_API_KEY=your-zhipu-key
LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
LLM_MODEL=glm-4-flash
```

### 方案 4：月之暗面 Kimi
```
LLM_API_KEY=your-moonshot-key
LLM_BASE_URL=https://api.moonshot.cn/v1
LLM_MODEL=moonshot-v1-8k
```

### 方案 5：通义千问（阿里）
```
LLM_API_KEY=your-dashscope-key
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_MODEL=qwen-plus
```

### 方案 6：Ollama 本地模型（完全免费）
```bash
# 先安装 Ollama 并启动服务
ollama serve
```
然后：
```
LLM_API_KEY=ollama
LLM_BASE_URL=http://localhost:11434/v1
LLM_MODEL=qwen2.5
```

> 任何提供 OpenAI 兼容接口的服务（vLLM、LM Studio、本地部署等）都可直接使用。

## 🧪 评估与测试

内置故障场景测试用例：

```bash
python scripts/evaluate.py
pytest -v
```

## 📁 项目结构

```
fault-diagnosis-agent/
├── main.py                       # FastAPI 入口
├── src/fault_diagnosis_agent/
│   ├── api.py                    # 路由定义
│   ├── config.py                 # 配置（含 LLM 多提供商支持）
│   ├── llm.py                    # LLM 可选客户端
│   ├── models.py                 # Pydantic 数据模型
│   ├── diagnosis.py              # RAG 推理管线与节点逻辑
│   ├── graph.py                  # LangGraph 状态机
│   ├── prompts.py                # 提示词
│   └── retrieval/                # 实体抽取、故障分类、混合检索
├── scripts/
│   ├── build_knowledge_base.py   # Word → JSON 知识库
│   ├── draw_graph.py             # 绘制诊断流程图
│   └── evaluate.py               # 评估脚本
├── data/
│   ├── processed/fault_knowledge.json
│   └── eval/test_cases.json
├── static/                       # 可视化前端（可选）
├── docs/                         # 附加文档
├── tests/                        # pytest 测试
├── requirements.txt
├── pyproject.toml
└── README.md
```

## 🔧 开发者提示

- **不要硬编码 API Key**：始终通过环境变量或 `.env` 文件注入。
- **离线部署**：不设置 `LLM_API_KEY` 时，Agent 自动降级为规则 + 知识库推理，可完全离线运行。
- **切换服务商**：仅需修改 `.env` 中的 `LLM_BASE_URL` 和 `LLM_MODEL`，无需修改任何代码。
- **扩展知识库**：`fault_knowledge.json` 是纯文本 JSON，可以手动编辑或从 Excel/其他格式转换导入。

## 📄 License

MIT
